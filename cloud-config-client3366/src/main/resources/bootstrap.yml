server:
  port: 3366

spring:
  application:
    name: config-client
  cloud:
    config:
      label: master   #分支名称
      name: config    #配置文件名称
      profile: dev    #dev读取后缀名称，总和上面三个实际读取路径为 http://localhost:9090/master/config-dev.yml
      uri: http://localhost:3344  #配置中心地址

  rabbitmq:
    host: 192.168.52.134
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"