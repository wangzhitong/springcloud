server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848  #服务注册中心地址
      config:
        file-extension: yaml  #指定yaml格式配置
        server-addr: localhost:8848 #配置中心地址
        group: DEV_GROUP
        namespace: 56559522-945c-411b-8fd7-416428116033
