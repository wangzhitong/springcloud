server:
  port: 8006

spring:
  application:
    name: cloud-consul-payment
  cloud:
    consul:
      host: 192.168.52.134
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        heartbeat:
          enabled: true